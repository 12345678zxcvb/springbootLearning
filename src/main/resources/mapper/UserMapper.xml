<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.situ.springboot.mapper.UserMapper">
    <!--List<User> selectAll();-->
    <select id="selectAll" resultType="User">
        SELECT * FROM User
    </select>
    <select id="selectById" resultType="User">
        SELECT id,name,password,email,phone,image,role from user where id = #{id}
    </select>

    <!--void deleteById(Integer id);-->
    <delete id="deleteById" parameterType="Integer">
        DELETE FROM user WHERE id=#{id}
    </delete>

    <insert id="add" parameterType="User">
        INSERT INTO user(name, password,email,phone,image, role) values (#{name},#{password},#{email},#{phone},#{image},#{role})
    </insert>

    <update id="update" parameterType="User">
        UPDATE user SET name=#{name},password=#{password},role=#{role},email=#{email},phone=#{phone},image=#{image}
        WHERE id=#{id}
    </update>

</mapper>